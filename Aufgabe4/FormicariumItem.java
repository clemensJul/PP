// TODO: is extenden von interfaces richtig? bzw. wie erbt man sonst in einem interface von einem anderen interface? (muss man vielleicht die struktur überdenken?)
public interface FormicariumItem extends Compatibility {
}
